Simple, flexible and lightweight SwiftUI implementation of coordinator pattern for iOS applications.

Currently supports navigation stack and tabs - the two main bricks in every iOS app's navigation.
