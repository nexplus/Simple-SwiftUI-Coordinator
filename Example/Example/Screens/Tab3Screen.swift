import SwiftUI

struct Tab3Screen: ScreenView {
    var body: some View {
        VStack(spacing: 40) {
            Text("Tab 3")
        }
        .background(.red)
   }
}

#Preview {
    Tab3Screen()
}
