import SwiftUI

struct RestorePasswordScreenView: ScreenView {
    var body: some View {
        VStack(spacing: 40) {
            Text("RestorePasswordScreenView")
        }
    }
}

#Preview {
    RestorePasswordScreenView()
}
